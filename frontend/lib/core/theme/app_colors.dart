import 'package:flutter/material.dart';

class AppColors {
  // Background & Surface Colors (Dark First)
  static const Color background = Color(0xFF0F1115);
  static const Color surface = Color(0xFF1A1D24);
  static const Color surfaceElevated = Color(0xFF232730);

  // Accent Colors
  static const Color primary = Color(0xFF4F46E5); // Electric Indigo
  static const Color primaryHover = Color(0xFF6366F1);
  static const Color secondary = Color(0xFF8B5CF6); // Subtle Purple/Blue

  // Text Colors
  static const Color textPrimary = Color(0xFFF8FAFC); // High Contrast White
  static const Color textSecondary = Color(0xFF94A3B8); // Muted Gray
  static const Color textHint = Color(0xFF475569);

  // Status Colors
  static const Color success = Color(0xFF10B981);
  static const Color successBackground = Color(0xFF064E3B);
  
  static const Color warning = Color(0xFFF59E0B);
  static const Color warningBackground = Color(0xFF78350F);
  
  static const Color error = Color(0xFFEF4444);
  static const Color errorBackground = Color(0xFF7F1D1D);
  
  static const Color info = Color(0xFF3B82F6);
  static const Color infoBackground = Color(0xFF1E3A8A);

  // Dividers & Borders
  static const Color border = Color(0xFF334155);
}
