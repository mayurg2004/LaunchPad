import 'package:flutter/material.dart';

class POCompaniesScreen extends StatelessWidget {
  const POCompaniesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Companies List Coming Soon'),
      ),
    );
  }
}
